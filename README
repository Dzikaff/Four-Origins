// Ne Ti Te Ni agree about time spatially.
// if iNFR is frequency then iSTR is amplitude.
// In spherical geometry directions are relative to altitude (elevation).
// Fe taajuus määrittyy sen mukaan miten eri ihmisten havaitaan kokevan eri tunteita reaktiona samaan ärsykkeeseen.
// Fe taajutta vastaanottaakseen suuria määriä pitää katsoa lukuisia eri ihmisiä nopeasti.
// Fe taajuuden vastaanottaminen on aktiivista kuten delfiinien kaikuluotausaisti.
// To value Fe is to value this frequency for its own sake.
// Unvalued strong Fe means this frequency is used as a means for something else.
// Elemental size can be measured in terms of frequency, amplitude and work.
// Frequency and amplitude are obvious. Even Super-Ego Si people perceive each other in terms of size according to
// frequency and amplitude.
// Work is a magnitude that isn't obvious. It isn't obvious how much work does it take to produce a Fe item.
// It is more obvious from a raw recording or raw data than from processed data or studio versions.
// It is even more obvious from a solo recording.
// Ni needs to be strong (or Ne?) to tell an improvised recording for a not improvised one.
// No, Ti needs to be strong?
// iNFR have difficulty telling this difference?
// Se needs to be two-dimensional?
// If one doesn't know how something has been made the value of work turns irrelevant.
// For some people, if one doesn't know *who* made something, this is distressing. Ni proving?
//
// Ii reception table
// Active freq		Passive freq 	Active amp 		Passive amp
// Fe 				Fi 				. 				.
// Te 				Ti 				. 				.
// Se 				Si 				. 				.
// Ne 				Ni 				. 				.
//
// Apparently amplitude reception is always passive.
// Therefore the notion of frequency inherently has greater frequency than the notion of amplitude.
//
// Ii reception priority table
// Frequency 		Amplitude
// Ne 				Ni ("What are the ramifications of this?")
// Se 				Si
// Ti 				Te
// Fi 				Te
//

// Ni frequency is discrete small number. Ne frequency is larger number, not necessarily discrete but probably.
// Ni frequency is weak, Ne frequency is strong.
// Ti frequency is strong, Te frequency is weak.
// Si frequency is weak, amplitude strong.
// Se frequency is strong, amplitude weak.
// Fe frequency is strong, amplitude weak.
// Fi frequency is weak, amplitude strong.

Synesteettinen illuusio, Ne

Tässä simulaatiossa X on tyttö-oletettu nainen joka on kokemassa hyvää karmaa.

X sanoo Y:lle: "Se mitä se (viitaten henkilöön Z) tekee on säteistä koostuva kiekko niin että sen kiekon keskus on se (Z) itse”

Z:n oleminen näyttää Ni:ltä

Mutta jos X sanoo Z:lle: "Se mitä sä teet on säteistä koostuva kiekko niin että sen kiekon keskus olet sinä itse” niin Z ei yleensä koe asiaa sillä tavalla.

Olettamus: Z on eNTR

Syy illuusioon: Jos Z opettaa jollekulle muulle uutta tietoa, kokemus on välttämättä Ni vaikka voi sisältää muitakin isotooppeja.
